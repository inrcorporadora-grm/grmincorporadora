import styled from 'styled-components';
import { altImageCSS } from '../styles';

export const ContainerCSS = styled.div`
  position: relative;
  user-select: none;

  width: 100%;
  height: 28rem;

  ${altImageCSS}
`;
